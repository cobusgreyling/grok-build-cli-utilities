name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Ruff lint
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

      - name: Mypy type check
        run: mypy src/grok_build_cli_utilities --ignore-missing-imports

      - name: Run tests with coverage
        run: |
          pytest -q --cov=src/grok_build_cli_utilities --cov-report=term-missing --cov-report=xml

      - name: Upload coverage to artifact (optional)
        uses: actions/upload-artifact@v7
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        with:
          name: coverage-report-${{ matrix.python-version }}
          path: coverage.xml

      - name: Verify wheel builds cleanly
        run: |
          python -m pip install build twine -q
          python -m build --wheel --outdir /tmp/dist-ci
          twine check /tmp/dist-ci/*.whl
